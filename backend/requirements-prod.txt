# Production dependencies (Render / Docker) – no test packages

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Configuration & validation
pydantic>=2.10.0
pydantic-settings>=2.6.0

# HTTP client
httpx>=0.28.0

# Financial data
yfinance>=1.5.0

# AI agents
crewai[google-genai]>=1.15.0

# Database & vector store
supabase>=2.10.0
chromadb>=1.1.0

# PDF export
fpdf2>=2.8.0

# Google Drive API (report PDF export)
google-api-python-client>=2.160.0
google-auth>=2.36.0

# Owner authentication gate – signed session cookies + WebAuthn/passkey verification
itsdangerous>=2.2.0
webauthn>=3.0.0

# Groww Trade API – read-only broker holdings (no order placement wired in)
growwapi>=1.5.0
