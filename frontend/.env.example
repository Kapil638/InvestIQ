# Backend API base URL (no trailing slash)
VITE_API_URL=http://localhost:8001/api/v1

# Google Sign-In client ID (owner login) — safe to expose client-side by design,
# same as any public OAuth client ID. Must match GOOGLE_SIGNIN_CLIENT_ID in backend/.env.
VITE_GOOGLE_SIGNIN_CLIENT_ID=
